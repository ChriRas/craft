# macOS
.DS_Store

# IDE
.idea/
.vscode/

# Claude Code local state (kept out of plugin distribution)
.claude/settings.local.json

# CRAFT per-session prime marker (written by /craft:prime, cleared on SessionStart)
.claude/plans/.primed

# CRAFT record of the bash the hooks ran with (rewritten by the SessionStart hook at every session start)
.claude/plans/.hook-env

# AI tooling local config
.ai/

# OS temp / logs
*.log
*.tmp

/research

# CRAFT local state (per-session markers, run lock, local settings, worktree handoff)
.claude/plans/.execute.lock
.craft/
